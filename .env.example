# JobAlert Web — Environment Variables
# Copy this to .env and fill in your values

# Database (SQLite default — no changes needed for local dev)
DATABASE_URL=sqlite:///./jobalert.db

# For Railway: set DATABASE_URL to a persistent volume path
# DATABASE_URL=sqlite:////data/jobalert.db
